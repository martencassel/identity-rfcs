# 1. Introduction

**1.1**

```text
User (Alice) -> printin.com (Gets password)
|			| access her photos
|			v
|		    my-photoso.com/login (with password
Client (Browser) -> my-photos.com: Server (protected resources), Passwords for Users
```
In the traditional *client-*server authentication model, the client
requests an access-restricted resource (protected resource) on the
server by authenticating with the *server* using the resource owner's
credentials.

- client
- server (the application)
- resource owner
- resources on the server

**1.2**In order to provide *third-party applications* access to
   restricted resources, the resource owner shares its credentials with
   the third party.

**1.3**This creates several problems and limitations:

**1.4***Third-party applications* are required to store the resource
      owner's credentials for future use, typically a password in
      clear-text.

**1.5**Servers are required to support password authentication, despite
      the security weaknesses inherent in passwords.

**1.6** *Third-party applications* gain overly broad access to the resource
      owner's protected resources, leaving resource owners without any
      ability to restrict duration or access to a limited subset of
      resources.

**1.7***Resource owners* cannot revoke access to an individual third party
      without revoking access to all third parties, and must do so by
      changing the third party's password.

**1.8**Compromise of any *third-party application* results in compromise of
      the end-user's password and all of the data protected by that
      password.

**1.9**OAuth addresses these issues by introducing an *authorization layer*
   and separating the *role of the client* from that of *the resource
   owner*.

**1.10**In OAuth, the client requests access to resources controlled
   by the *resource owner* and hosted by the *resource server*, and is
   issued a different set of credentials than those of the resource
   owner.


**1.11**Instead of using the resource owner's credentials to access protected
   resources, the client obtains an access token -- a string denoting a
   specific scope, lifetime, and other access attributes.

	client credentials = (specific scope, lifetime, other access attributes)
**1.12**Access tokens
   are issued to *third-party clients* by an *authorization server* with the
   *approval of the *resource owner.

**1.13**The client uses the access token to
   access the protected resources hosted by the resource server.

**1.14**   For example, an end-user (resource owner) can grant a printing
   service (client) access to her protected photos stored at a photo-
   sharing service (resource server), without sharing her username and
   password with the printing service.

	- end-user (resource owner) Alice
		- 1. Can grant myprint.com access to her photo album to print them for her.
	- photo service (myphotos.com) / alice-photo-album (resource server)
		- Holds Alice protected photes in its resource server
	- printing service (myprinter.com) (client, get delegation-based credentials, granted)
		- Can print protected photos alice hosts on photo service
	- authorization server (sso.myphotoso.com)
**1.15** Instead, she authenticates
   directly with a server trusted by the photo-sharing service
   (authorization server), which issues the printing service delegation-
   specific credentials (access token).

**1.16**This specification is designed for use with HTTP ([RFC2616]).

**1.17**The use of OAuth over any protocol other than HTTP is out of scope.

**1.18**   The OAuth 1.0 protocol ([RFC5849]), published as an informational
   document, was the result of a small ad hoc community effort.

**1.19**This Standards Track specification builds on the OAuth 1.0 deployment
   experience, as well as additional use cases and extensibility
   requirements gathered from the wider IETF community.

**1.20**The OAuth 2.0 protocol is not backward compatible with OAuth 1.0.

**1.21**The two versions
   may co-exist on the network, and implementations may choose to
   support both.  However, it is the intention of this specification
   that new implementations support OAuth 2.0 as specified in this
   document and that OAuth 1.0 is used only to support existing
   deployments.  The OAuth 2.0 protocol shares very few implementation
   details with the OAuth 1.0 protocol.  Implementers familiar with
   OAuth 1.0 should approach this document without any assumptions as to
   its structure and details.



## 1.1. Roles

**1.**OAuth defines four roles:

**2.**resource owner

**3.**An entity capable of granting access to a protected resource.

**4.**When the resource owner is a person, it is referred to as an end-user

**5.**resource server

**6.**The server hosting the protected resources, capable of accepting
and responding to protected resource requests using access tokens.

**7.**client

**8.**An application making protected resource requests on behalf of the
resource owner and with its authorization

**9.**The term "client" does not imply any particular implementation characteristics (e.g.,
whether the application executes on a server, a desktop, or other devices).

**10.**authorization server

**11.**The server issuing access tokens to the client after successfully
authenticating the resource owner and obtaining authorization.

**12.**The interaction between the authorization server and resource server
is beyond the scope of this specification.

**13.**The authorization server may be the same server as the resource server or a separate entity.

**14.**A single authorization server may issue access tokens accepted by multiple resource servers.

## Analysis goes here:

## Verbs
Granting Access
Authenticating the Resource Owner
Obtaining Authorization from the Resource Owner
Issuing Access Tokens

## Nouns
Protected Resource
Access Token
Protected Resource Request

## Subjects (Entities)
Resource Owner (If a person, end-user)
Resource Server(s)
Authorization Server


## 1.2 Protocol Flow

## 1.1
The abstract OAuth 2.0 flow illustrated in Figure 1 describes the
interaction between the four roles and includes the following steps

## 1.2
A. Authorization Request (Client to Resource Owner)
B. Authorization Grant (Resource Owner to Client)
C. Authorization Grant (Client to Authorization Server)
D. Access Token (Authorization Server to Client)
E. Access Token (Client to Resource Server)
F. Protected Resource (Resource Server to Client)

- Authorization Grant = A credential representing the resource owners authorization
- Expression of Authorization Grant = Using one of four grant types
- Grant Types = Expressing Authorization Grant
		Depends on the method used by the client to request authorization
		and the types supported by the authorization server

## 1.3.  Authorization Grant

**1.1**An authorization grant is a credential representing the resource
   owner's authorization (to access its protected resources) used by the
   client to obtain an access token.

**1.2**This specification defines four
   grant types -- authorization code, implicit, resource owner password
   credentials, and client credentials -- as well as an extensibility
   mechanism for defining additional types.

## 1.3.1.  Authorization Code

**1.**   The authorization code is obtained by using an authorization server
   as an intermediary between the client and resource owner.

**2.**Instead of
   requesting authorization directly from the resource owner, the client
   directs the resource owner to an authorization server (via its
   user-agent as defined in [RFC2616]), which in turn directs the
   resource owner back to the client with the authorization code.

**3.**  Before directing the resource owner back to the client with the
   authorization code, the authorization server authenticates the
   resource owner and obtains authorization.

**4.** Because the resource owner
   only authenticates with the authorization server, the resource
   owner's credentials are never shared with the client.

**5.**   The authorization code provides a few important security benefits

**6.**   such as the ability to authenticate the client,

**7.**as well as the transmission of the access token directly to the client without
   passing it through the resource owner's user-agent

**8.**and potentially exposing it to others, including the resource owner.

## 1.3.4. Client Credentials

**1.1**  The client credentials (or other forms of client authentication) can
   be used as an *authorization grant* when the authorization scope is
   limited to the protected resources under the control of the client,
   or to protected resources previously arranged with the authorization
   server.

**1.2**Client credentials are used as an authorization grant
   typically when the client is acting on its own behalf (the client is
   also the resource owner) or is requesting access to protected
   resources based on an authorization previously arranged with the
   authorization server.

## 1.4 Access Token


## 1.1
Access tokens are credentials used to access protected resources.

**1.2**An access token is a string representing an authorization issued to the
   client.

   [wikipedia]
   - Authorization = Specyfing the rights/privileges for accessing resources
   - 		   = Deciding whether a particular subject has privilege to access
   		     a particular resource

   - Access, Scope, Duration

**1.3**The string is usually opaque to the client.

**1.4**Tokens represent specific scopes and durations of access, granted by the
   resource owner, and enforced by the resource server and authorization
   server.

**1.5** The token may denote an identifier used to retrieve the authorization
   information or may self-contain the authorization information in a
   verifiable manner (i.e., a token string consisting of some data and a
   signature).

**1.6**Additional authentication credentials, which are beyond
   the scope of this specification, may be required in order for the
   client to use a token.

**1.7**   The access token provides an abstraction layer, replacing different
   authorization constructs (e.g., username and password) with a single
   token understood by the resource server.

**1.8**This abstraction enables
   issuing access tokens more restrictive than the authorization grant
   used to obtain them, as well as removing the resource server's need
   to understand a wide range of authentication methods.

**1.9**   Access tokens can have different formats, structures, and methods of
   utilization (e.g., cryptographic properties) based on the resource
   server security requirements.

**1.10**Access token attributes and the
   methods used to access protected resources are beyond the scope of
   this specification and are defined by companion specifications such
   as [RFC6750].

## 1.5. Refresh Token

**1.1**Refresh tokens are credentials used to obtain access tokens.

**1.2**Refresh
   tokens are issued to the client by the authorization server and are
   used to obtain a new access token when the current access token
   becomes invalid or expires, or to obtain additional access tokens
   with identical or narrower scope (access tokens may have a shorter
   lifetime and fewer permissions than authorized by the resource
   owner).

**1.3** Issuing a refresh token is optional at the discretion of the
   authorization server.

**1.4** If the authorization server issues a refresh
   token, it is included when issuing an access token (i.e., step (D) in
   Figure 1).

**1.5** A refresh token is a string representing the authorization granted to
   the client by the resource owner.

**1.6**The string is usually opaque to the client.

**1.7** The token denotes an identifier used to retrieve the
   authorization information

**1.8**Unlike access tokens, refresh tokens are
   intended for use only with authorization servers and are never sent
   to resource servers.

## Refresh Token Flow:

A. Authorization Grant: A -> Authorization Server

B. Access Token:        Authorization Server -> Client
B. Refresh Token: 	Authorization Server -> Refresh Token

C. Access Token:	Client -> Resource Server
D. Protected Resource:  Resource Server -> Client
E. Access Token: 	Client -> Resource Server
F. Invalid Token Error: Resource Server -> Client

G. Refresh Token:       Client -> Authorization Server
H. Access Token:	Authorization Server -> Client

## 1.7 HTTP Redirections

**1.1**
   This specification makes extensive use of HTTP redirections, in which
   the client or the authorization server directs the resource owner's
   user-agent to another destination.

**1.2** While the examples in this
   specification show the use of the HTTP 302 status code, any other
   method available via the user-agent to accomplish this redirection is
   allowed and is considered to be an implementation detail.

## 2. Client Registration

**1.1**   Before initiating the protocol, the client registers with the
   authorization server.

**1.2**The means through which the client registers
   with the authorization server are beyond the scope of this
   specification but typically involve end-user interaction with an HTML
   registration form.

**1.3**   Client registration does not require a direct interaction between the
   client and the authorization server.

**1.4** When supported by the
   authorization server, registration can rely on other means for
   establishing trust and obtaining the required client properties
   (e.g., redirection URI, client type).

**1.5**For example, registration can
   be accomplished using a self-issued or third-party-issued assertion,
   or by the authorization server performing client discovery using a
   trusted channel.

**1.6**   When registering a client, the client developer SHALL:

**1.7**specify the client type as described in Section 2.1,

**1.8**provide its client redirection URIs as described in Section 3.1.2,

**1.9** include any other information required by the authorization server
      (e.g., application name, website, description, logo image, the
      acceptance of legal terms).

## 2.1 Client Types

**1.1**   OAuth defines two client types, based on their ability to
   authenticate securely with the authorization server (i.e., ability to
   maintain the confidentiality of their client credentials):

**1.2**confidential

**1.3**Clients capable of maintaining the confidentiality of their
credentials (e.g., client implemented on a secure server with
restricted access to the client credentials), or capable of secure
client authentication using other means.

**1.4**public

**1.5**Clients incapable of maintaining the confidentiality of their
credentials (e.g., clients executing on the device used by the
resource owner, such as an installed native application or a web
browser-based application), and incapable of secure client
authentication via any other means.

**1.6**   The client type designation is based on the authorization server's
   definition of secure authentication and its acceptable exposure
   levels of client credentials.

**1.7**The authorization server SHOULD NOT make assumptions about the client type.

**1.8**A client may be implemented as a distributed set of components, each
   with a different client type and security context (e.g., a
   distributed client with both a confidential server-based component
   and a public browser-based component).

**1.9**If the authorization server
   does not provide support for such clients or does not provide
   guidance with regard to their registration, the client SHOULD
   register each component as a separate client.

**1.10**client profile

This specification has been designed around the following client
   profiles:

**1.11**web application

**1.12**A web application is a confidential client running on a web server.

**1.13**Resource owners access the client via an HTML user interface rendered
in a user-agent on the device used by the resource owner.

**1.14**The client credentials as well as any access token issued to the client
are stored on the web server and are not exposed to or accessible by the resource owner.


**1.15**user-agent-based application

**1.16**A user-agent-based application is a public client in which the
client code is downloaded from a web server and executes within a
      user-agent (e.g., web browser) on the device used by the resource
      owner.

**1.17**Protocol data and credentials are easily accessible (and
often visible) to the resource owner.

**1.18**Since such applications reside within the user-agent, they can make seamless use of the
user-agent capabilities when requesting authorization.

**1.19**native application

**1.20**A native application is a public client installed and executed on the device used by the resource owner.

**1.21**Protocol data and credentials are accessible to the resource owner.

**1.22**It is assumed that any client authentication credentials included in the
application can be extracted.

**1.23**On the other hand, dynamically issued credentials such as access tokens or refresh tokens can
receive an acceptable level of protection.

**1.24**At a minimum, these credentials are protected from hostile servers with which the
application may interact.  On some platforms, these credentials
might be protected from other applications residing on the same
device.

## 2.2 Client Identifier

**1.1**The authorization server issues the registered client a client
   identifier -- a unique string representing the registration
   information provided by the client.

**1.2**The client identifier is not a
   secret; it is exposed to the resource owner and MUST NOT be used
   alone for client authentication.

**1.3**The client identifier is unique to
   the authorization server.

**1.4**   The client identifier string size is left undefined by this
   specification.

**1.5**The client should avoid making assumptions about the
   identifier size.

**1.6**The authorization server SHOULD document the size
   of any identifier it issues.

## 2.3.  Client Authentication

**1.1**If the client type is confidential, the client and authorization
server establish a client authentication method suitable for the
security requirements of the authorization server.

The authorization server MAY accept any form of client authentication meeting its
security requirements.

**1.2**Confidential clients are typically issued (or establish) a set of
client credentials used for authenticating with the authorization
server (e.g., password, public/private key pair).

**1.3**The authorization server MAY establish a client authentication method
with public clients.

**1.4**However, the authorization server MUST NOT rely
on public client authentication for the purpose of identifying the
client.

**1.5**The client MUST NOT use more than one authentication method in each
request.

## OAuth Protocol Flow (Grant Type: Code Authorization Grant )

End-User
  - 1. Click on printing server

User Agent (Browser) {
  - 1. Direct user to printing.service.com
}

## My photos.com
func ResourceServer() {
  5. grant client resource only if access token is valid
}

func AuthorizationServer() {
   1. Handle Authorization Request
   2. Direct User Agent to Authorization Page
   3. Wait for user to decide permissions granted
   4. Redirect user agent back to Client, grant authorization grant (Code)
   5. Wait for client to exchange code for access token
   6. Get
}

func ClientAppServer() {
  - 1. Redirect user agent to  "MyPhotos.com"
  - 2. Get code grant via user agent from user.
  - 3. call authorization server, to exchange code grant for access token.
  - 4. call resource server using access token.
}

## Traditional Server Flow

End-User {
 1. Enter app server url
 2. Enter username / password
 3. Use resource page
}

User Agent (Browser) {
 1. On link click, send agent to application server using HTTP GET
 2. Get redirect for /login page
 3. Send HTTP POST /application/login
 4. Send HTTP request to application resource page
}

Application Server {
  handle / login(username, password) {
     return session (cookie and server) if authenticate(username, password)
  }

  handle / resource {
    get session, redirect to login if no session
    check authorization in session, deny if not permitted
  }
}

Third Party Application {
  handle / get_resource_owners(credential) {
     store users credential
  }

  call application server {
     perform service onbehalf, HTTP GET Application Server.handle/resource,
     get session using resource owners credential
  }
}
